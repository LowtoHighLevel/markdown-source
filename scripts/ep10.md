# Episode 10 Script


Over the last few videos, we have been designing a custom CPU and computer, emulator, and assembler. Last time, we wrote our first program in assembly. Our CPU is relatively simple in how it works, and cannot do any advanced math. Today, I would like to write some code in assembly that can help with that.


Also, hello! This is a new character instead of my usual face. I wanted to try a few new things and a new editing style.

(swipe to text saying math): First, I would like to introduct an easier way to do some bit shifting. Right now, we can do what is called a rotate. Let's look at an example:

(animate this)

This is an eight-bit number, with our carry flag. Let's set our carry flag to 1. We can also set the first bit to 1.

When we do a rotate to the left, all of our bits move over to the left, with the carry flag moving into the first bit.

Now we have a 0 in the carry flag, and the binary equivelant of 2 in our number. This is a built in feature to our CPU. If we wanted to do a regular bit shift, we would not want the carry flag in place. We want our input to be a zero. This means that we will have to clear that flag somehow. An easy way to do that is to do some sort of operation that will ensure that the carry flag will be set to 0 before doing our rotate.

This will do our initial bit shift.

Let's try to implement this in code (swipe to text saying code then to video)

(implementation done unscripted)

Now we have some basic bit shifting, which will prove very useful for our next goals in the next episode. We'll work on how we can do some multiplication and division then! That's all the time I have for today, so I will see you in the next one!